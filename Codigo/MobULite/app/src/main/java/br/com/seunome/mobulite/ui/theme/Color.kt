package br.com.seunome.mobulite.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand — Teal ───────────────────────────────────────────────────────────
val AppTeal         = Color(0xFF0D9488)  // teal-600  — primary action
val AppTealDark     = Color(0xFF0F766E)  // teal-700  — pressed / emphasis
val AppTealDarker   = Color(0xFF115E59)  // teal-800  — deep accent
val AppTealMedium   = Color(0xFF14B8A6)  // teal-400  — avatars / light accent
val AppTealLight    = Color(0xFFCCFBF1)  // teal-100  — chip / container bg
val AppTealBg       = Color(0xFFECFDF5)  // teal-50   — section background

// ── Semantic — Green (success / online) ────────────────────────────────────
val AppGreen        = Color(0xFF16A34A)  // green-600
val AppGreenDark    = Color(0xFF15803D)  // green-700
val AppGreenDarker  = Color(0xFF065F46)  // green-900
val AppGreenEmerald = Color(0xFF047857)  // emerald-700
val AppGreenLight   = Color(0xFFD1FAE5)  // emerald-100
val AppGreenBg      = Color(0xFFF0FDF4)  // green-50
val AppGreenOnline  = Color(0xFF22C55E)  // green-500  — live indicator dot

// ── Semantic — Red (error / cancel) ────────────────────────────────────────
val AppRed          = Color(0xFFDC2626)  // red-600
val AppRedDark      = Color(0xFF991B1B)  // red-800
val AppRedDarker    = Color(0xFF7F1D1D)  // red-900
val AppRedLight     = Color(0xFFFEE2E2)  // red-100

// ── Semantic — Amber (warning / ETA) ───────────────────────────────────────
val AppAmber        = Color(0xFFD97706)  // amber-600
val AppAmberBright  = Color(0xFFF59E0B)  // amber-500
val AppAmberOrange  = Color(0xFFEA7600)  // orange    — driver-arriving
val AppAmberDark    = Color(0xFF92400E)  // amber-800
val AppAmberLight   = Color(0xFFFEF3C7)  // amber-100

// ── Semantic — Blue (navigation / info) ────────────────────────────────────
val AppBlue         = Color(0xFF2563EB)  // blue-600
val AppBlueDark     = Color(0xFF1E40AF)  // blue-800
val AppBlueSky      = Color(0xFF1ABCFE)  // sky accent
val AppBlueLight    = Color(0xFFDBEAFE)  // blue-100
val AppBlueBg       = Color(0xFFEFF6FF)  // blue-50
val AppSkyBg        = Color(0xFFF0F9FF)  // sky-50

// ── Brand — Violet/Lilac (from logo) ───────────────────────────────────────
val AppPurple        = Color(0xFF7C3AED)  // violet-600  — logo purple, primary action
val AppVioletDark    = Color(0xFF6D28D9)  // violet-700  — pressed / emphasis
val AppVioletDarker  = Color(0xFF5B21B6)  // violet-800  — deep accent
val AppLilac         = Color(0xFFA78BFA)  // violet-400  — soft lilac accent
val AppLilacMedium   = Color(0xFF8B5CF6)  // violet-500  — medium lilac
val AppPurpleLight   = Color(0xFFEDE9FE)  // violet-100  — chip / container bg
val AppLilacSoft     = Color(0xFFF5F3FF)  // violet-50   — section background
val AppIndigo        = Color(0xFF4F46E5)  // indigo-600
val AppIndigoLight   = Color(0xFFEEF2FF)  // indigo-50

// ── Neutral — Slate ────────────────────────────────────────────────────────
val Slate50         = Color(0xFFF8FAFC)
val Slate100        = Color(0xFFF1F5F9)
val Slate150        = Color(0xFFF5F7FA)  // between 50 and 100
val Slate200        = Color(0xFFE2E8F0)
val Slate300        = Color(0xFFCBD5E1)
val Slate400        = Color(0xFF94A3B8)
val Slate500        = Color(0xFF64748B)
val Slate600        = Color(0xFF475569)
val Slate700        = Color(0xFF334155)
val Slate800        = Color(0xFF1E293B)
val Slate850        = Color(0xFF111827)  // gray-900 (extra deep)
val Slate900        = Color(0xFF0F172A)

// ── Rating ─────────────────────────────────────────────────────────────────
val RatingStarYellow = Color(0xFFFFC107)

